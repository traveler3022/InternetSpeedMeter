# بانک جامع ایده‌ها، تکنیک‌ها و الگوهای استخراج‌شده از تمامی پروژه‌ها (Ideas & Techniques Bank)

> **هدف این سند:** تجمیع کامل و موشکافانه تمامی ایده‌ها، خلاقیت‌های فنی، تکنیک‌های بهینه‌سازی و راه‌حل‌های حل باگ که از پروژه‌های مختلف (`netindicator`، `NetSpeed`، `gasflow`، `netspeed-monitor` و AOSP) استخراج شده است تا به عنوان مرجع کامل طراحی و ارتقای برنامه استفاده شود.

---

## ۱. ایده‌های مدیریت مصرف باتری و پایش هوشمند (Battery & Lifecycle Ideas)

### ایده ۱: الگوریتم خواب عمیق با حفظ دلتای پس‌زمینه (Zero-Wakeup Background Traffic Accumulator)
* **منبع ایده:** ترکیب تکنیک صرفه‌جویی باتری در `NetSpeed` و نیاز به عدم از دست رفتن دیتای دانلودهای شبانه در `InternetSpeedMeter`.
* **نحوه کارکرد:**
  - هنگامی که صفحه خاموش می‌شود (`ACTION_SCREEN_OFF`)، حلقه ۱ ثانیه‌ای کاملاً متوقف می‌شود تا پردازنده هیچ بیداری (CPU Wakeup) غیرضروری نداشته باشد.
  - اما شمارنده‌های بایت فیزیکی کرنل ذخیره می‌مانند.
  - به محض روشن شدن صفحه (`ACTION_SCREEN_ON`)، بلافاصله تفاضل ترافیک مصرفی در طول خواب استخراج شده و به حجم روزانه کاربر افزوده می‌شود.
  - **نتیجه:** صفر درصد مصرف باتری در حالت صفحه خاموش، اما با دقت ۱۰۰ درصدی در ثبت حجم مصرفی دانلودها، استریم موزیک و آپدیت‌ها.

### ایده ۲: انطباق با حالت ذخیره نیرو (Power Save Mode Adaptive Throttling)
* **منبع ایده:** پروژه `netindicator` (مجوز MIT).
* **نحوه کارکرد:**
  - گوش دادن به برودکست سیستمی `PowerManager.ACTION_POWER_SAVE_MODE_CHANGED`.
  - بررسی متد `powerManager.isPowerSaveMode()`.
  - هنگامی که شارژ گوشی کم است و کاربر حالت Battery Saver را روشن می‌کند، سرعت‌سنج به جای متوقف شدن یا مصرف زیاد، فرکانس نمونه‌برداری را از ۱ ثانیه به ۳ یا ۵ ثانیه تغییر می‌دهد (Adaptive Polling).

### ایده ۳: وضعیت اولیه صفحه در هنگام بوت (Headless Cold-Boot Awareness)
* **نحوه کارکرد:**
  - در زمان بالا آمدن گوشی پس از ریبوت، صفحه معمولاً خاموش و گوشی داخل جیب است.
  - با فراخوانی `(getSystemService(PowerManager::class.java)).isInteractive` در متد شروع، وضعیت اولیه چک می‌شود. اگر صفحه خاموش باشد، حلقه محاسباتی اصلاً استارت نمی‌خورد و منتظر اولین تعامل کاربر می‌ماند.

---

## ۲. ایده‌های دقت محاسبات شبکه و رفع تداخلات (Network Precision & Attribution Ideas)

### ایده ۴: حذف تداخل و شمارش دوبل فیلترشکن‌ها و VPN با NetworkCallback
* **منبع ایده:** پروژه `netindicator` (مجوز MIT).
* **نحوه کارکرد:**
  - اکثر برنامه‌های عادی از `TrafficStats.getTotalRxBytes()` استفاده می‌کنند. در زمان روشن بودن VPN، بسته‌ها یک بار روی اینترفیس مجازی `tun0` و یک بار روی اینترفیس فیزیکی `wlan0` شمرده می‌شوند و سرعت و حجم مصرفی ۲ برابر ثبت می‌شود!
  - **راه حل:** استفاده از `ConnectivityManager.NetworkRequest.Builder()` با فیلترهای:
    ```kotlin
    .addCapability(NetworkCapabilities.NET_CAPABILITY_INTERNET)
    .addCapability(NetworkCapabilities.NET_CAPABILITY_NOT_VPN)
    ```
  - با این کار، ترافیک فقط از روی لایه سخت‌افزاری واقعی ردیابی می‌شود و فیلترشکن‌ها سرعت را دستکاری نمی‌کنند.

### ایده ۵: تفکیک قطعی و ضدخطای وای‌فای از سیم‌کارت (Capability-Based Network Routing)
* **منبع ایده:** تحلیل علل خطای منفی شدن `TrafficStats.getMobileRxBytes()`.
* **نحوه کارکرد:**
  - به جای تفریق کورکورانه آمار متزلزل سیم‌کارت از کل، نوع اینترنت فعال با `NetworkCapabilities` سنجیده می‌شود:
    - اتصال فعال `TRANSPORT_WIFI` یا `TRANSPORT_ETHERNET` باشد $\rightarrow$ تمام دلتای ترافیک به وای‌فای نسبت داده می‌شود.
    - اتصال فعال `TRANSPORT_CELLULAR` باشد $\rightarrow$ تمام دلتای ترافیک به موبایل اختصاص می‌یابد.
  - این کار خطای چیپست‌ها و رام‌های کاستوم در متد `getMobileRxBytes` را ۱۰۰٪ خنثی می‌کند.

### ایده ۶: حل باگ جهش ناگهانی سرعت هنگام جابجایی شبکه (Network Switching Interface Drop)
* **منبع ایده:** کامیت `1550221` در `netindicator`.
* **نحوه کارکرد:**
  - هنگامی که کاربر از وای‌فای به دیتا تغییر وضعیت می‌دهد، سیستم‌عامل ممکن است اینترفیس جدید را معرفی کند بدون اینکه سریعاً رویداد قطع اینترفیس قبلی صادر شود.
  - نگهداری جدول اینترفیس‌ها و تخلیه (Drop) مقادیر اینترفیس منقضی‌شده باعث می‌شود از پرش‌های غیرواقعی چند صد مگابایتی در لحظه سوییچ شبکه جلوگیری شود.

### ایده ۷: محاسبه میلی‌ثانیه‌ای با کلاک یکنواخت (Monotonic Zero-Drift Clock)
* **منبع ایده:** پروژه‌های `netindicator` و `NetSpeed`.
* **نحوه کارکرد:**
  - استفاده قطعی از `SystemClock.elapsedRealtime()` به جای `System.currentTimeMillis()`.
  - محاسبه سرعت بر اساس زمان واقعی سپری‌شده میلی‌ثانیه‌ای:
    $$\text{Speed (B/s)} = \frac{\Delta \text{Bytes} \times 1000}{\text{CurrentTimeMs} - \text{PreviousTimeMs}}$$
  - ارزیابی مستقل آپلود و دانلود به طوری که دانلود خالص (مانند استریم ویدیوی UDP) هرگز سرعت را صفر نکند.

---

## ۳. ایده‌های رندر پیکسلی و نمایشی آیکون استاتوس‌بار (Status Bar Graphics & Rendering Ideas)

### ایده ۸: تفکیک دو کانال فورگراند و آیکون (Dual Notification Channel Decoupling)
* **منبع ایده:** پروژه `netindicator` (مجوز MIT).
* **نحوه کارکرد:**
  - **کانال ۱ (Service Channel):** با اهمیت `IMPORTANCE_NONE` برای پاس کردن الزامات فورگراند سرویس در اندروید ۱۴ بدون ایجاد نوتیفیکیشن مزاحم.
  - **کانال ۲ (Throughput Channel):** با اهمیت `IMPORTANCE_DEFAULT` و قابلیت `setOnlyAlertOnce(true)` برای نمایش مداوم آیکون زنده در نوار وضعیت گوشی.

### ایده ۹: تطبیق پیکسلی با دانسیته نمایشگر (Density-Aware Canvas Layout)
* **منبع ایده:** کامیت `f0c67b6` در `netindicator`.
* **نحوه کارکرد:**
  - آیکون وضعیت اندروید ۲۴ در ۲۴ دی‌پی (dp) است.
  - محاسبه ابعاد بر مبنای تراکم گوشی: `val iconSize = (density * 24).toInt().coerceIn(24, 96)`
  - در گوشی‌های مختلف (از صفحه‌های کوچک تا رزولوشن‌های 4K و 2K)، آیکون با بالاترین وضوح کشیده می‌شود و سیستم‌عامل مجبور به بلور کردن بیت‌مپ نمی‌شود.

### ایده ۱۰: سازگاری ۱۰۰٪ با تم تاریک و روشن با فرمت `ALPHA_8`
* **منبع ایده:** شاخه `new_main_activity` در `NetSpeed`.
* **نحوه کارکرد:**
  - ساخت بیت‌مپ با کانفیگ `Bitmap.Config.ALPHA_8` به جای `ARGB_8888`.
  - در این حالت، بیت‌مپ فقط شامل کانال آلفا (ماسک شفافیت) است و به صورت ذاتی به سیستم‌عامل اجازه می‌دهد آیکون را در صفحه‌های روشن مشکی، و در صفحه‌های تیره سفید کند (سازگاری کامل با تم دارک و لایت بدون باگ سفید روی سفید).

### ایده ۱۱: تراز عمودی با FontMetrics و مقیاس‌پذیری فونت (Auto-Scaling Font)
* **نحوه کارکرد:**
  - با استفاده از متدهای `paint.fontMetrics` (محاسبه ascent و descent)، متن در مرکز عمودی تراز می‌شود تا از بریدگی بالا و پایین ارقام جلوگیری شود.
  - اگر سرعت به ارقام ۳ رقمی (مانند ۹۹۹) برسد، با بررسی `measureText`، اندازه فونت به صورت خودکار مقیاس‌بندی شده و فشرده می‌شود تا متن از کادر ۲۴ دی‌پی بیرون نزند.

### ایده ۱۲: جلوگیری از نشت حافظه با کپی ایمیوتبل و `recycle()`
* **منبع ایده:** پروژه `netindicator`.
* **نحوه کارکرد:**
  - در هر ثانیه بیت‌مپ جدید در حافظه ساخته نشود، یا بلافاصله پس از بسته شدن درون کلاس `Icon` با متد `recycle()` از حافظه آزاد گردد تا Garbage Collector دچار وقفه نشود.

---

## ۴. ایده‌های معماری دیتابیس و ارتباط میان پروسه‌ها (Architecture & Data Persistence Ideas)

### ایده ۱۳: کش دولایه هوشمند (Dual-Layer Persistence)
* **نحوه کارکرد:**
  - کش سریع در `SharedPreferences` در فواصل کوتاه جهت مقابله با خاموش شدن ناگهانی گوشی.
  - نوشتن بافرشده (Throttled) در دیتابیس Room SQLite در فواصل ۵ یا ۱۰ ثانیه‌ای جهت حفظ سلامت حافظه فلش (NAND Flash) گوشی و جلوگیری از داغ شدن دستگاه.

### ایده ۱۴: معماری ارتباط زنده با اکتیویتی از طریق Messenger IPC
* **منبع ایده:** شاخه `origin/new_main_activity` در `NetSpeed`.
* **نحوه کارکرد:**
  - اگر کاربر داخل محیط برنامه باشد، اکتیویتی می‌تواند از طریق یک `Messenger` و `ServiceConnection` به سرویس پس‌زمینه وصل شود و سرعت لحظه‌ای را به صورت پیام‌های سبک `MSG_UPDATE_SPEED` دریافت کند، بدون اینکه نیاز به خواندن مجدد دیتابیس داشته باشد.

### ایده ۱۵: مدیریت اتمیک رول‌اور نیمه‌شب (Midnight Atomicity)
* **نحوه کارکرد:**
  - گوش دادن همزمان به `ACTION_DATE_CHANGED` و ارزیابی تغییر روز در حلقه ترافیک.
  - هنگام عبور از نیمه‌شب، رکورد روز قبل با تمام ترافیک بسته می‌شود و برای روز جدید رکورد اولیه با حجم صفر ایجاد می‌گردد تا آمار روزها با هم تداخل پیدا نکند.

---
این سند به عنوان بانک جامع ایده‌ها و تجارب برای پیاده‌سازی بک‌اند نهایی ثبت گردید.
