# پرونده تحلیل جامع ۴ ایجنت: کالبدشکافی معماری، باگ‌ها و قابلیت‌های پروژه‌های سرعت‌سنج اینترنت

> **تاریخ گزارش:** ۲۰ سپتامبر ۲۰۲۶  
> **روش‌شناسی:** تحلیل موازی ۴ ایجنت تخصصی (NetSpeed Auditor, NetIndicator Auditor, Upstream Forensic Auditor, Current Backend Auditor)  
> **هدف:** استخراج تمامی تجارب، متدولوژی‌های حل مسئله، بررسی باگ‌های پنهان، خطاهای معماری و تدوین استاندارد جامع برای پیاده‌سازی نهایی بک‌اند **InternetSpeedMeter** بدون دستکاری UI.

---

## فهرست سرفصل‌های گزارش
1. [گزارش ایجنت اول: کالبدشکافی کامل پروژه NetSpeed (شاخه‌های master و new_main_activity)](#1-گزارش-ایجنت-اول-کالبدشکافی-کامل-پروژه-netspeed)
2. [گزارش ایجنت دوم: کالبدشکافی معماری پروژه NetIndicator (پروژه MIT بدون وابستگی)](#2-گزارش-ایجنت-دوم-کالبدشکافی-معماری-پروژه-netindicator)
3. [گزارش ایجنت سوم: باستان‌شناسی فنی نسخه اصلی جاوا (vaibhavpallod/InternetSpeedMeter)](#3-گزارش-ایجنت-سوم-باستانشناسی-فنی-نسخه-اصلی-جاوا)
4. [گزارش ایجنت چهارم: ممیزی خط به خط بک‌اند کاتلین فعلی (InternetSpeedMeter)](#4-گزارش-ایجنت-چهارم-ممیزی-خط-به-خط-بکاند-کاتلین-فعلی)
5. [جدول مقایسه جامع ماتریس قابلیت‌ها و باگ‌ها در هر ۴ پروژه](#5-جدول-مقایسه-جامع-ماتریس-قابلیتها-و-باگها)
6. [نقشه راه اجرایی و نیازمندی‌های قطعی پیاده‌سازی بک‌اند نوین](#6-نقشه-راه-اجرایی-و-نیازمندیهای-قطعی-پیادهسازی-بکاند-نوین)

---

## ۱. گزارش ایجنت اول: کالبدشکافی کامل پروژه NetSpeed

### ۱.۱ وضعیت حقوقی و لایسنس (یافته بسیار مهم)
* بر خلاف تصور اولیه، پروژه [NetSpeed](file:///root/NetSpeed) تحت لایسنس آزاد GPL نیست!
* نویسنده در کامیت `b42c02f` در فایل README اعلام کرده:
  > *"No longer maintained here. Note that the code here is copyrighted. DO NOT USE WITHOUT EXPLICIT PERMISSION."*
* بنابراین این کد به عنوان نرم‌افزار انحصاری (Proprietary) محسوب می‌شود. **طبق قوانین بین‌المللی کپی‌رایت، هرگونه کپی‌برداری از کد منبع آن غیرقانونی است** و تنها باید از طریق متدولوژی **Clean-Room Design** (استفاده از ایده‌ها و معماری بدون استفاده از حتی یک خط کد) بازپیاده‌سازی شود.

### ۱.۲ ساختار فایل‌ها و چرخه حیات (شاخه master)
* **اجرا در پروسس مجزا (`android:process=":background"`):**
  سرویس [IndicatorService.java](file:///root/NetSpeed/app/src/main/java/com/nisargjhaveri/netspeed/IndicatorService.java) در یک پروسه ایزوله لینوکس اجرا می‌شود تا کرش لایه UI باعث توقف سرویس نشود؛ اما این کار حافظه رم پایه را به دلیل لود شدن دو نسخه از رانتایم جاوا/اندروید دو برابر می‌کند (۳۰ الی ۵۰ مگابایت اورهد اضافه).
* **چرخه زمانی ۱ ثانیه‌ای:**
  با استفاده از یک `Handler` و رانبل `mHandlerRunnable` که هر ثانیه با `postDelayed(1000)` صدا زده می‌شود.
* **محاسبه سرعت و باگ ساعت وال‌کلاک (`System.currentTimeMillis`):**
  فرمول `(usedBytes * 1000) / usedTime` برای سرعت استفاده می‌شود؛ اما به دلیل استفاده از `System.currentTimeMillis()` (به جای کلاک یکنواخت `SystemClock.elapsedRealtime()`)، در صورت تغییر ساعت گوشی توسط کاربر، تنظیمات شبکه NTP یا تغییر ساعت تابستانی، مقدار `usedTime` منفی یا صفر شده و سرعت لحظه‌ای صفر نشان داده می‌شود!
* **محدودیت‌های فرمت‌بندی سرعت:**
  - سرعت‌های زیر ۱ کیلوبایت در ثانیه به دلیل تقسیم صحیح به عدد ۰ رند شده و اپ `0 kB/s` نشان می‌دهد.
  - سرعت‌های بالاتر از ۱۰۰ مگابایت بر ثانیه (یا ۱۰۰ مگابیت) به صورت هاردکد روی متن `+99` فریز می‌شوند و در شبکه‌های 5G و Wi-Fi 6 غیرقابل استفاده هستند.

### ۱.۳ مدیریت باتری در NetSpeed
* استفاده از رسیور داینامیک `mScreenBroadcastReceiver`:
  - به `ACTION_SCREEN_OFF` گوش می‌دهد و سریعاً `pauseNotifying()` را فراخوانی کرده و کال‌بک‌های Handler را حذف می‌کند تا پردازنده به خواب برود.
  - به `ACTION_SCREEN_ON` و `ACTION_USER_PRESENT` گوش داده و قفل بودن صفحه را با `KeyguardManager.isKeyguardLocked()` می‌سنجد.
* **باگ پرش سرعت در بیداری (Sleep Delta Bug):** اگر صفحه ۴ ساعت خاموش باشد و فایلی در بک‌گراند دانلود شود، در لحظه روشن شدن صفحه، تفاضل کل ۴ ساعت تقسیم بر زمان ۴ ساعت شده و سرعت میانگین ۴ ساعت در ثانیه اول نشان داده می‌شود به جای سرعت لحظه‌ای.

### ۱.۴ نوآوری شاخه `origin/new_main_activity` (ارتباط IPC با Messenger)
* در شاخه `new_main_activity`، کدهای ارتباط بین UI و سرویس از طریق **Android Messenger و Binder IPC** بازنویسی شده است:
  - کلاس [IndicatorServiceConnector.java](file:///root/NetSpeed/app/src/main/java/com/nisargjhaveri/netspeed/IndicatorServiceConnector.java) روی یک `HandlerThread` اختصاصی ارتباط دوطرفه برقرار می‌کند.
  - پیام‌های `MSG_REGISTER_CLIENT` و `MSG_UPDATE_SPEED` سرعت لحظه‌ای را به اکتیویتی می‌فرستند.
  - فرمت بیت‌مپ آیکون از `ARGB_8888` به `ALPHA_8` تغییر یافته تا تم‌های دارک و لایت استاتوس‌بار اندروید بتوانند بدون افت کیفیت آن را Tint کنند.

---

## ۲. گزارش ایجنت دوم: کالبدشکافی معماری پروژه NetIndicator

### ۲.۱ ویژگی‌های شاخص فنی [netindicator](file:///root/netindicator)
* **بدون هیچ وابستگی خارجی (Zero Dependencies):** فاقد کتابخانه‌های سنگین؛ صرفاً با فریم‌ورک پایه اندروید و لایسنس فوق‌العاده باز MIT توسعه داده شده است.
* **سازگاری کامل با اندروید ۱۳ و ۱۴:**
  - استفاده از دسترسی‌های `POST_NOTIFICATIONS` و `FOREGROUND_SERVICE_SPECIAL_USE`.
  - ثبت تایپ سرویس `android:foregroundServiceType="specialUse"` در مانیفست.

### ۲.۲ تفکیک دو کانال نوتیفیکیشن (Dual Notification Channels)
* **کانال اول (`NOTIFICATION_CHANNEL_SERVICE`):** اهمیت `IMPORTANCE_NONE`، متصل به `startForeground()` سرویس به عنوان لنگر پایدار فورگراند در اندروید ۱۴ بدون ایجاد مزاحمت بصری برای کاربر.
* **کانال دوم (`NOTIFICATION_CHANNEL_THROUGHPUT`):** اهمیت `IMPORTANCE_DEFAULT` با قابلیت `setOnlyAlertOnce(true)` جهت به‌روزرسانی مداوم آیکون دوخطی سرعت در استاتوس‌بار.

### ۲.۳ ردیابی مبتنی بر اینترفیس و حذف تداخل VPN
* به جای خواندن آمار کلی سیستم (`TrafficStats.getTotalRxBytes()`) که ترافیک لوپ‌بک و وی‌پی‌ان را دوبار می‌شمارد، از `ConnectivityManager.NetworkCallback` با فیلترهای:
  - `NET_CAPABILITY_INTERNET`
  - `NET_CAPABILITY_NOT_VPN`
  استفاده می‌کند و بایت‌ها را مستقیماً از روی نام اینترفیس سخت‌افزاری (`TrafficStats.getTxBytes(iface)`) می‌خواند.
* **حل باگ جابجایی شبکه در کامیت `1550221`:** هنگام سوییچ از وای‌فای به دیتا یا جابجایی دکل‌ها، اینترفیس‌های تکراری و قدیمی فوراً نال می‌شوند تا جهش‌های ناگهانی در محاسبه رخ ندهد.

### ۲.۴ کشف ۲ باگ پنهان در کد netindicator توسط ایجنت
1. **باگ گارد شرطی در ترافیک یک‌طرفه:**
   در خط ۳۷۷ فایل `NetIndicatorService.java`:
   ```java
   if (millis > lastMillis[i] && txBytes > lastTxBytes[i] && rxBytes > lastRxBytes[i])
   ```
   به دلیل استفاده از عملگر `&&` میان آپلود و دانلود، اگر ترافیک فقط در یک جهت جریان داشته باشد (مثلاً دانلود صرف ویدیوی بدون آپلود بسته)، شرط نقض شده و نرخ ترافیک ۰ نشان داده می‌شود! (باید با عملگر `||` ارزیابی گردد).
2. **باگ معکوس بودن فلگ ذخیره انرژی در کامیت `24a00e8`:**
   در فراخوانی `indicator.stop(isPowerSaveMode)`، اگر حالت ذخیره باتری روشن باشد، متد `stop(true)` را صدا می‌زند که برعکس باعث می‌شود آیکون روی صفحه فریز شده باقی بماند!

### ۲.۵ شاهکار بهینه‌سازی حافظه و رندر آیکون (کامیت `f0c67b6`)
* استفاده از فونت سفارشی فشرده ۲۰ کیلوبایتی `asap_subset.ttf`.
* محاسبه ابعاد آیکون بر اساس تراکم پیکسلی صفحه: `iconSize = (int) density * 24`.
* محاسبه دقیق مختصات رسم عمودی با `Paint.FontMetrics` و جلوگیری از بریدگی بالا و پایین متن.
* استفاده از کپی ایمیوتبل بیت‌مپ و فراخوانی بلادرنگ `recycle()` برای صفر کردن فشار روی Garbage Collector رانتایم.

---

## ۳. گزارش ایجنت سوم: باستان‌شناسی فنی نسخه اصلی جاوا (`vaibhavpallod`)

### ۳.۱ تاریخچه کامیت‌ها و چرایی شکست پروژه اصلی
بررسی شاخه `upstream/master` و `upstream/Vaibhav-1` در مخزن نشان می‌دهد که نویسنده هندی پروژه اصلی بین ۹ ژانویه تا ۶ فوریه ۲۰۲۱ تلاش داشته برنامه‌ای پایدار بسازد، اما به دلیل خطاهای فاحش فنی در کامیت `2bac956` پروژه را رها کرده است:

1. **فیلتر اشتباه مانیفست:** تگ `<intent-filter>` برای `BOOT_COMPLETED` داخل تگ `<service>` تعریف شده بود و پرمیشن `RECEIVE_BOOT_COMPLETED` نیز اضافه نشده بود؛ در نتیجه سرویس در نسخه اصلی **هرگز با بوت گوشی شروع به کار نمی‌کرد!**
2. **سوءاستفاده شدید از `startForeground`:** در حلقه ۱ ثانیه‌ای، هر ثانیه به جای `notify()` تابع `startForeground(1, ...)` صدا زده می‌شد که در سیستم‌عامل اندروید خطای نقض استاندارد است.
3. **فاجعه تایمر ساعت ۱۸:۰۷ عصر:**
   یک تایمر `java.util.Timer` با ساعت هاردکد `18:07:30` (دقیقاً ساعت تست سازنده پشت میزش در تاریخ ۶ فوریه ۲۰۲۱) تنظیم شده بود که قرار بود ریست شبانه را انجام دهد!
4. **تلاش برای سریالایز کردن سرویس اندروید با Gson در WorkManager:**
   سازنده تلاش کرده بود کل آبجکت `NotificationService` (شامل کانتکست و بوم و پینت) را با Gson تبدیل به JSON کند و داخل Data وورکر بگذارد که با پرتاب خطای سقف ۱۰ کیلوبایت وورک‌منیجر کرش می‌کرد. نویسنده خطوط دی‌سریالایز را در کامیت `2bac956` کامنت کرد و باعث شد متغیر نال بماند و کلاس `ResetWork` به محض فراخوانی خطای `NullPointerException` پرتاب کند.
5. **نادیده گرفتن ۱۰۰ درصدی دیتای وای‌فای و هاردکد عدد ۳۰۰:**
   در کد جاوا فقط `getMobileRxBytes()` خوانده می‌شد و در دیتابیس برای وای‌فای عدد ثابت هاردکد `"300"` نوشته می‌شد!
6. **باگ فرمول سرعت کل:**
   در `Speed.java`، شرط‌ها به جای سرعت کل، متغیر `upspeed` را بررسی می‌کردند:
   `else if (upspeed >= 1000000)` و `else if (upspeed >= 1000)`. اگر کاربر ۵۰ مگابایت دانلود می‌کرد و آپلود کمتر از ۱ کیلوبایت بود، سرعت کل **۰ کیلوبایت** ثبت می‌شد!
7. **سرریز عدد صحیح در محاسبه تاریخ (Integer Overflow):**
   در فرمول `c.setTime(c.getTime() + 86400000 * i)` با رسیدن `i` به ۲۵، ضرب در میلی‌ثانیه روز از حد `Integer.MAX_VALUE` بیشتر شده و تاریخ به گذشته جهش منفی می‌کرد!
8. **کرش `IndexOutOfBoundsException` در RecyclerView اکتیویتی:**
   کد اکتیویتی لیست‌های موازی را در صورت وجود بیش از یک رکورد در دیتابیس با خطای Out of bounds متوقف می‌کرد.

---

## ۴. گزارش ایجنت چهارم: ممیزی خط به خط بک‌اند کاتلین فعلی

بررسی عمیق فایل‌های کاتلین در [InternetSpeedMeter](file:///root/InternetSpeedMeter/app/src/main/java/com/vsp/internetspeedmeter/) نشان داد اگرچه معماری مدرن شده، اما **۷ نقص و باگ بحرانی** وجود دارد:

### باگ ۱: نابودی دیتای ترافیک پس‌زمینه (Background Data Loss)
* **محل باگ:** در [InternetService.kt](file:///root/InternetSpeedMeter/app/src/main/java/com/vsp/internetspeedmeter/broadcastreceiver/InternetService.kt#L55-L65)
* **مکانیزم:** در زمان خاموشی صفحه پایش لغو می‌شود. وقتی صفحه مجدداً روشن می‌شود، متد `syncTrafficStats()` مستقیماً شمارنده‌های پایه (`prevTotalRx`) را با بایت‌های جدید بازنویسی می‌کند. در نتیجه تمام دیتایی که در طول خاموشی صفحه مصرف شده (دانلود شبانه، استریم موسیقی، به‌روزرسانی اپ‌ها) **به طور کامل نابود شده و هرگز به دیتابیس مصرف روزانه کاربر اضافه نمی‌شود.**

### باگ ۲: خطای محاسبه سرعت به علت دریفت زمانی (Speed Inaccuracy via Time Drift)
* **محل باگ:** خطوط ۱۳۱-۱۳۲ در `InternetService.kt`
* **مکانیزم:** سرعت با فرض اینکه تاخیر حلقه همواره ۱۰۰۰ میلی‌ثانیه است، صرفاً با تفاضل بایت‌ها محاسبه می‌شود. با درگیر شدن دیسک یا کشش CPU، زمان چرخه به ۱.۵ یا ۲ ثانیه افزایش می‌یابد و سرعت تا ۲ برابر بیشتر از واقعیت به کاربر گزارش می‌شود.

### باگ ۳: تله شمارنده صفر در زمان راه‌اندازی (Zero-Rx Initialization Trap)
* **محل باگ:** خطوط ۱۲۲-۱۲۹ در `InternetService.kt`
* **مکانیزم:** شرط `if (prevTotalRx == 0L || ...)` بر صفر بودن بایت‌های ورودی تکیه دارد. پس از ریبوت گوشی، تا زمانی که بایت‌ها صفر است برنامه در چرخه اسکیپ مداوم گیر می‌افتد.

### باگ ۴: خطای تفکیک وای‌فای و موبایل با اعداد منفی
* **محل باگ:** خطوط ۱۳۱-۱۴۰ در `InternetService.kt`
* **مکانیزم:** متد `TrafficStats.getMobileRxBytes()` در برخی چیپست‌ها مقدار `-1` (`UNSUPPORTED`) برمی‌گرداند که بعد از تبدیل به صفر، کل دیتای موبایل به اشتباه به عنوان دیتای وای‌فای ثبت می‌شود. باید اتصال قطعی از `ConnectivityManager.NetworkCapabilities` اعتبارسنجی شود.

### باگ ۵: فرض اشتباه روشن بودن صفحه هنگام بوت سیستم
* **محل باگ:** خط ۳۸ در `InternetService.kt` (`private var isScreenOn = true`)
* **مکانیزم:** هنگام ریبوت گوشی در جیب، سرویس فرض می‌کند صفحه روشن است و پردازنده را مداوم با فرکانس ۱ ثانیه درگیر می‌کند. باید با `PowerManager.isInteractive` سنجیده شود.

### باگ ۶: مشکلات رندر آیکون در دانسیته‌های مختلف
* **محل باگ:** فایل [NotificationService.kt](file:///root/InternetSpeedMeter/app/src/main/java/com/vsp/internetspeedmeter/NotificationService.kt)
* **مکانیزم:** استفاده از ابعاد ثابت ۹۶×۹۶ بدون توجه به رزولوشن گوشی، و نادیده گرفتن متریکس خط پایه فونت (`Paint.FontMetrics`) که منجر به تاری یا بریدگی ارقام در سرعت‌های بالا می‌شود.

### باگ ۷: عدم تقارن دیتابیس و شردپفرنس در رول‌اور نیمه‌شب
* **محل باگ:** متد `checkDateRollover()` فقط در حلقه فعال ارزیابی می‌شود؛ اگر گوشی هنگام نیمه‌شب با صفحه خاموش باشد، دیتای قبل و بعد از ۱۲ شب با بیداری صبح دچار اختلال محاسباتی می‌شود.

---

## ۵. جدول مقایسه جامع ماتریس قابلیت‌ها و باگ‌ها

| ویژگی / شاخص فنی | نسخه اولیه جاوا (`vaibhavpallod`) | NetSpeed (`nisargjhaveri`) | NetIndicator (`pgaskin`) | وضعیت فعلی ما | نسخه ارتقا یافته ما |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **زبان و الگو** | Java قدیمی | Java سنتی | Java تمیز و سبک | Kotlin + Coroutines | Kotlin مدرن + StateFlow/Coroutines |
| **لایسنس** | رها شده / نامشخص | انحصاری (حق کپی‌رایت محفوظ) | MIT آزاد | اختصاصی | توسعه تمیز (Clean-Room) |
| **پایداری داده (Room DB)** | ❌ (باگ شدید و نال) | ❌ (ندارد) | ❌ (ندارد) | ✅ (دارد) | ✅ (دارد - پایدار و اتمیک) |
| **حفظ ترافیک زمان خاموشی صفحه** | ❌ | ❌ | ❌ | ❌ (باگ نابودی دیتا) | ✅ (الگوریتم نمونه‌برداری پیوسته) |
| **محاسبه دقیق با کلاک یکنواخت** | ❌ (دریفت کامل) | ⚠️ (وال‌کلاک متزلزل) | ✅ (`SystemClock.elapsedRealtime`) | ❌ (تفاضل فرضی ۱ ثانیه) | ✅ (`SystemClock.elapsedRealtime`) |
| **تفکیک دقیق سیم‌کارت و وای‌فای** | ❌ (وای‌فای هاردکد ۳۰۰) | ❌ (فقط دیتای کل) | ❌ (فقط اینترفیس فعال) | ⚠️ (ناقص با خطای چیپست) | ✅ (اعتبارسنجی با NetworkCapabilities) |
| **مدیریت دانسیته آیکون استاتوس‌بار** | ❌ (ثابت) | ❌ (ثابت ۹۶×۹۶) | ✅ (پویا `density * 24`) | ❌ (ثابت ۹۶×۹۶) | ✅ (پویا بر مبنای دانسیته + فونت متریکس) |
| **پشتیبانی از PowerSaveMode** | ❌ | ❌ | ✅ | ❌ | ✅ |
| **سازگاری ۱۰۰٪ با لایه UI فعلی** | - | - | - | - | ✅ (بدون تغییر حتی ۱ خط در UI) |

---

## ۶. نقشه راه اجرایی و نیازمندی‌های قطعی پیاده‌سازی بک‌اند نوین

### نیازمندی‌های قطعی و قواعد عدم تغییر UI:
1. **دست نزدن به لایه UI:** فایل‌های `MainActivity.kt`، `UsageAdapter.kt` و تمامی لی‌اوت‌های XML به صورت ۱۰۰٪ دست‌نخورده باقی می‌مانند.
2. **تطابق کامل انتیتی Room:** کلاس `Usage` فیلدهای `date`, `mobile`, `wifi`, `total` را دقیقاً با فرمت مورد نیاز لایه UI فراهم خواهد کرد.
3. **موتور محاسبه بدون دریفت زمانی:** در `InternetService.kt`، از `SystemClock.elapsedRealtime()` برای محاسبه دقیق میلی‌ثانیه‌ای استفاده می‌شود.
4. **الگوریتم حفظ ترافیک پس‌زمینه (Zero Data Loss):** در متد تغییر وضعیت صفحه، ابتدا دلتای بایت‌های مصرف‌شده در زمان خاموشی استخراج، به حجم امروز افزوده شده و سپس شمارنده‌ها به‌روزرسانی می‌شوند.
5. **مدیریت هوشمند اینترفیس:** ادغام با `ConnectivityManager` جهت اطمینان از انتساب درست بایت‌ها به وای‌فای یا موبایل.
6. **رندر بهینه آیکون:** تنظیم داینامیک بر مبنای دانسیته و فونت متریکس برای نمایش کاملاً خوانا در استاتوس‌بار.

این گزارش جامع‌ترین مرجع فنی جهت بازنویسی و حل قطعی تمامی مشکلات بک‌اند برنامه است.
